# Material Processing Pipeline Requirements

# OCR & Image Processing
pytesseract>=0.3.10
Pillow>=10.0.0

# PDF Processing
PyMuPDF>=1.23.0

# Document Processing
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.21
striprtf>=0.0.26
xlrd<2.0.0

# Video Processing
opencv-python>=4.8.0

# Fast numerical operations
numpy>=1.24.0

# System resource monitoring (for dynamic worker scaling)
psutil>=5.9.0

# Local Whisper (FREE transcription)
# faster-whisper uses CTranslate2 for 4-8x speed over openai-whisper
# GPU: float16, CPU: int8 quantization
# Requires PyTorch (installed separately) for CUDA detection
faster-whisper>=1.1.0

# Web Interface (API Server)
flask>=3.0.0
flask-cors>=4.0.0
