# Vocality ASR — pipeline dependencies.
#
# torch + torchvision + torchaudio installed manually (CUDA-specific for 1080 Ti):
#   pip install torch==2.5.1+cu118 torchvision==0.20.1+cu118 torchaudio==2.5.1+cu118 \
#       --index-url https://download.pytorch.org/whl/cu118
# (torch 2.5.1 is the last Pascal-friendly build compatible with transformers 5.x.)
#
# The rest installs via `pip install -r requirements.txt`.

# --- ASR ---
faster-whisper>=1.0.0
ctranslate2>=4.0.0
whisperx>=3.1.0                  # WhisperX + wav2vec2 alignment

# --- Speaker stack ---
pyannote.audio>=3.1,<4.0
pyannote.metrics>=3.2
silero-vad>=5.0

# --- Vocal isolation ---
audio-separator[gpu]>=0.22.0     # MelBand Roformer wrapper

# --- Pitch / audio ---
librosa>=0.10
pyworld>=0.3.4
soundfile>=0.12
numpy>=1.26,<2.0

# --- LLM polish ---
anthropic>=0.40.0                # only when POLISH_ENGINE = "api"

# --- QA / benchmarking ---
jiwer>=3.0                       # WER / CER
scipy>=1.12                      # diarization metrics

# --- Utilities ---
psutil>=5.9                      # CPU affinity pinning
tqdm>=4.66
pynvml>=11.5                     # GPU compute-capability probe

# --- Windows GPU runtime (CTranslate2 needs CUDA 12 cuBLAS + cuDNN 8) ---
nvidia-cuda-runtime-cu12
nvidia-cublas-cu12
nvidia-cudnn-cu11<9

# --- pyannote embedder backend (1.x breaks under pytorch-lightning stack walk) ---
speechbrain<1.0

# --- Test ---
pytest>=8.0
