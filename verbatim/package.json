{
  "name": "verbatim",
  "version": "0.1.0",
  "description": "Local GUI for the Verbatim ASR pipeline — vocal-lesson transcription with per-person voiceprints",
  "main": "main.js",
  "author": "Ionuț Roșu",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "node --test \"tests/**/*.test.js\"",
    "fetch-ffmpeg": "cd .. && node scripts/fetch-ffmpeg.js",
    "build-engine": "cd .. && .venv\\Scripts\\python.exe -m PyInstaller --noconfirm --distpath verbatim build-engine.spec",
    "build": "electron-builder --config build-config/electron-builder.yml",
    "build-win": "electron-builder --win --config build-config/electron-builder.yml",
    "build-all-win": "npm run fetch-ffmpeg && npm run build-engine && npm run build-win",
    "publish-win": "electron-builder --win --publish always --config build-config/electron-builder.yml"
  },
  "dependencies": {
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "electron": "^41.0.0",
    "electron-builder": "^26.0.0"
  }
}
