{
  "name": "vocality",
  "version": "0.1.0",
  "description": "Local GUI for the Vocality ASR pipeline — vocal-lesson transcription with per-person voiceprints",
  "main": "main.js",
  "author": "Ionuț Roșu",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "node --test \"tests/**/*.test.js\"",
    "build": "electron-builder --config build-config/electron-builder.yml",
    "build-win": "electron-builder --win --config build-config/electron-builder.yml"
  },
  "dependencies": {
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "electron": "^41.0.0",
    "electron-builder": "^26.0.0"
  }
}
